name: Test tool mode native loop input
# Tests that the tool_mode=native_loop input and associated tool harness
# configuration inputs are accepted by the action without input validation
# errors. The action should fail at the precheck step (missing REPO/PR_NUMBER)
# rather than at input parsing.
on:
  workflow_dispatch:

jobs:
  test-tool-mode:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install GitHub CLI (gh) if not present
        run: |
          if ! command -v gh &>/dev/null; then
            curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg \
              | dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
            chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
            echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" \
              | tee /etc/apt/sources.list.d/github-cli.list > /dev/null
            apt-get update -qq
            apt-get install -y -qq gh
          fi
          gh --version

      - name: Run action with tool_mode=native_loop
        id: action
        uses: .
        continue-on-error: true
        with:
          github_token: fake-token-for-testing
          ai_base_url: http://localhost:11434/v1
          ai_model: test-model
          tool_mode: native_loop
          tool_max_requests: "6"
          tool_max_rounds: "4"
          tool_loop_wall_clock_sec: "180"
          tool_loop_summarize: "false"
          tool_failure_enforcement: "false"
          tool_enable_for_forks: "false"
          repo: ""
          pr_number: ""

      - name: Assert action failed at precheck (not at input validation)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail at precheck, got: $OUTCOME"
            exit 1
          fi
          echo "PASS: tool_mode=native_loop inputs were accepted; action failed at precheck as expected"
